# 🧊 冰箱守卫者 (Fridge Guardian)

> **您的智能食材管家 —— 让每一份食材都物尽其用，拒绝浪费，美味加倍。**

## 📖 项目简介

**冰箱守卫者 (Fridge Guardian)** 是一款基于 **Spring Boot** 和 **AI 技术** 的智能食材管理系统。

在快节奏的生活中，我们经常忘记冰箱里有什么，导致食材过期浪费，或者面对一堆剩菜不知道吃什么。本项目旨在解决这些痛点：它不仅能帮助用户**可视化管理库存**、**自动监控保质期**，还能利用 **AI 大模型** 根据现有食材**智能生成创意食谱**，助您开启绿色、健康的饮食生活。

---

## ✨ 核心功能与亮点

### 1. 🛡️ 智能库存与“保质期看板”

不再需要手动翻找冰箱，系统提供清晰的食材档案库。

* **可视化录入**：支持分类（蔬菜、肉类、水果等）、数量、存放位置（冷藏/冷冻/常温）的详细记录。
* **智能倒计时**：后端自动计算剩余保质天数，通过公式 `(购买日期 + 保质期天数) - 当前日期` 精确追踪每一份食材的状态。
* **色彩预警 UX**：前端采用直观的颜色系统区分紧迫程度（如红色代表3天内过期，黄色代表1周内过期），一眼识别“急需处理”的食材。

### 2. ⏰ 全天候自动化临期巡检

系统如同您的私人管家，每日自动运行巡检任务。

* **自动扫描**：系统后台每天定时扫描数据库中的所有库存。
* **主动提醒**：筛选出即将过期的项目，并通过“今日必吃”弹窗或模拟通知机制主动触达用户，确保没有任何食材被遗忘。

### 3. 🤖 “剩菜救星” —— AI 灵感配餐

这是本项目的核心黑科技功能，解决“今晚吃什么”的世纪难题。

* **一键求助 AI**：用户只需勾选冰箱里现有的、特别是即将过期的食材（例如：西红柿、鸡蛋、芹菜），点击“求助AI”。
* **大模型驱动**：后端无缝对接先进的 AI 大语言模型（如 DeepSeek），将食材列表转化为烹饪指令。
* **优雅展示**：AI 生成的定制化菜谱包含详细步骤，经过 Markdown 解析后以优雅易读的格式呈现在前端，让烹饪变得简单有趣。

### 4. 📊 数据可视化与浪费统计

帮助用户建立更健康的消费习惯。

* **浪费统计图表**：集成专业图表库（ECharts），直观展示本月因过期未消耗而造成的浪费情况及预估经济损失。
* **移动端适配**：优化的界面设计确保在手机浏览器上也能流畅操作，完美贴合厨房使用场景。

---

## 🛠️ 技术栈概览

本项目采用前后端分离的设计思想（逻辑层面），实现了完整的技术闭环。

### 后端架构 (Backend)

* **核心框架**: Spring Boot 3.x —— 提供稳健的业务逻辑支持。
* **任务调度**: Spring Scheduled —— 实现定时巡检功能。
* **AI 集成**: RESTful API 调用 —— 连接外部 AI 模型接口。
* **持久层**: 数据库 (Database) —— 存储食材档案与用户数据。

### 前端交互 (Frontend)

* **模板引擎**: Thymeleaf —— 实现服务端渲染与动态页面展示。
* **数据可视化**: ECharts —— 构建交互式统计图表。
* **Markdown 渲染**: 用于美化 AI 生成的菜谱文本。
* **响应式设计**: 适配移动端与桌面端体验。

---

## 🚀 项目价值

1. **经济价值**：减少因遗忘导致的食材过期浪费，为用户省钱。
2. **环保意义**：减少食物浪费，践行绿色生活方式。
3. **技术实践**：本项目完整涵盖了从 CRUD 业务、定时任务、第三方 AI 接口集成到数据可视化的全流程开发，具有极高的学习与实践参考价值。
