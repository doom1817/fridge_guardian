# “冰箱守卫者”——智能临期食品管理与“剩菜”菜谱推荐系统说明文档 (Smart Fridge Guardian)

## 一、项目简介

“冰箱守卫者”是一个智能临期食品管理与“剩菜”菜谱推荐系统，旨在通过AI技术解决家庭食材浪费问题。系统结合Spring Boot后端逻辑、定时任务及AI接口调用，实现对冰箱内临期食材的管理、智能提醒与菜谱生成。

## 二、创新价值与生活意义

1. **解决痛点**：针对现代人因忙碌而忘记冰箱内食材，导致食物浪费的普遍问题。
2. **AI结合点**：与传统仅支持数据录入的管理系统不同，本系统能够利用AI模型（如DeepSeek或GPT）根据用户冰箱内现有的、即将过期的食材，一键生成定制化的“清冰箱菜谱”。
3. **技术闭环**：系统涵盖了Spring Boot标准业务逻辑，并集成定时任务（过期自动提醒）与外部AI API调用，形成完整的技术实现闭环，具有较高的学习与实践成就感。

## 三、四天开发计划（AI加速版）

### 第一天：食材档案库与“保质期看板”

- **功能**：实现食材信息的增删改查（CRUD）。核心数据字段包括：食材名称、分类（如肉类/蔬菜/水果）、购买日期、保质期天数。
- **业务逻辑**：后端自动计算剩余保质天数，公式为：`剩余天数 = (购买日期 + 保质期天数) - 当前日期`。
- **UI/UX重点**：在Thymeleaf前端页面中，使用颜色直观区分食材的紧迫程度（例如：3天内过期显示为红色，1周内过期显示为黄色）。
- **AI辅助应用**：利用AI辅助设计数据库表结构（Schema），并生成初始化食材数据的SQL脚本（例如，包含常见蔬菜水果的默认保质期数据）。

### 第二天：定时任务与消息通知

- **功能**：系统于每日早上9点自动运行定时任务，扫描数据库中所有食材，筛选出即将过期的项目。
- **核心技术**：使用Spring Boot的 `@Scheduled`注解实现定时任务。
- **生活化体现**：接入通知机制。可模拟发送邮件通知，或在系统首页设计一个“今日必吃”弹窗，主动提示用户。
- **AI辅助应用**：利用AI生成标准的Spring Boot定时任务代码模板。

### 第三天：核心黑科技——AI剩菜救星（重点）

- **功能**：用户可在前端页面勾选多种即将过期的食材（例如：西红柿、鸡蛋、芹菜），点击“求助AI”按钮。
- **核心技术**：后端调用AI API（推荐使用高性价比的DeepSeek接口），将用户选择的食材列表发送给AI模型。
- **Prompt示例**：`“我有以下食材：[西红柿、鸡蛋、芹菜]，请帮我设计一个简单好做的菜谱，并给出烹饪步骤。”`
- **结果展示**：将AI返回的Markdown格式文本进行解析，并在前端页面以优雅、易读的方式展示生成的菜谱。

### 第四天：数据可视化与优化

- **功能**：

	1. **浪费统计**：使用ECharts图表库，可视化展示本月因食材过期未消耗而造成的预估经济损失（基于未在过期前标记为“已吃完”的食材进行计算）。
	2. **移动端适配**：优化Thymeleaf模板，确保在手机浏览器上也能便捷地进行食材录入与查看操作（贴合厨房场景中多使用手机的特点）。

- 

	**AI辅助应用**：利用AI辅助编写ECharts图表的配置JavaScript代码。

key：sk-1f7adfd6ef99498599bf9edd01d0118c